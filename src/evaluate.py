"""src/evaluate.py
Metric computation & plotting utilities.
Rules mandate that all figures must be stored under `.research/iteration2/images`.  No
change is required inside this module because the caller (`main.py`) now passes an
absolute path that already conforms to the required directory layout.
"""