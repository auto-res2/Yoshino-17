experiment_name: smoke_test_hirma
random_seeds: [111]
run_mode: smoke
hardware:
  device: cpu
models:
  dummy_local:
    engine: dummy  # lightweight backend for the smoke test
    name_or_path: dummy
    tensor_parallel_size: 1
    temperature: 0.7
llm_apis: {}
guardrails:
  - none
  - pat
  - tarpo
  - hirma
datasets:
  jailbreakbench:
    hf_repo: llm-safety/jailbreakbench
    split: test[:10]
  jambench:
    hf_repo: llm-safety/jambench
    split: test[:10]
metrics:
  - multi_turn_asr
  - single_turn_asr
output_dir: .research/iteration2  # kept for backward-compat; not used by code
